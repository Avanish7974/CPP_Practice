# find the lcm and hcf between 3 input taken from user
age =  int(input("Enter your age : "))
if(age >= 18):

    print("You can vote  ")
else:
    print("You can not vote")

    # hame jo output chahiye wo naa mile is called bug
    # and if error is in code then error